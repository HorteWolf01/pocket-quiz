<!DOCTYPE html>
<html>
  <head>
    <title>Pocket Quiz 2.0</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <p class="d-flex flex-wrap align-items-center">
        <%= link_to "Pocket Quiz 2", '/', :class => 'text-decoration-none text-white me-auto display-6' %>
        <% if $user %>
          <%= link_to $user.name, '/login', :class => 'mx-3 text-decoration-none text-white' %>
        <% else %>
          <%= link_to "Log In", '/login', :class => 'mx-3 text-decoration-none text-white' %>
        <% end %>
        <%= link_to "Editor", "/editor/quizzes", :class => 'btn btn-secondary' %>
      </p>
    </header>

    <% flash.each do |type, msg| %>
      <div class="alert alert-info">
        <%= msg %>
      </div>
    <% end %>

    <br><br>

    <div class="bg-white container">
      <%= yield %>
    </div>
      
<style>
.footer {
  position: fixed;
  height: 85px;
  width: 100%;
  left: 0;
  bottom: 0;
}
</style>

    <div class="footer">
      <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <p class="lead mx-4 text-muted">&copy; 2022 Dzmitry Ramanouski, BSUIR</p>
      </footer>
    </div>
  </body>
</html>
